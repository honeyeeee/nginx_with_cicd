services:
  node:
    build: .
    image: myapp
    ports:
      - "4000:4000"
    volumes:
      - .:/app

  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - node
    restart: always  